# Data pipeline (sections 3.1-3.8 of the README). These are all that is needed
# to reproduce the manifests from the raw corpus.
numpy>=2.0
pandas>=2.2
Pillow>=10.0
lxml>=5.0
pytest>=8.0

# Training stack (roadmap items 1-5, not yet implemented).
# Install the torch build matching your CUDA version; see https://pytorch.org
torch>=2.4
torchvision>=0.19
editdistance>=0.8      # CER / WER
matplotlib>=3.8        # scaling-curve plots
tqdm>=4.66
